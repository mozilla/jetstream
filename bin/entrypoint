#!/usr/bin/env python3

from pensieve.experimenter import ExperimentCollection
import datetime as dt


last_week = dt.datetime.now(dt.timezone.utc) - dt.timedelta(weeks=1)
collection = ExperimentCollection.from_experimenter().started_since(last_week)

print([e.slug for e in collection.experiments])
